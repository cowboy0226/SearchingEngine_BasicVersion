基础功能版本：
功能：
    构建倒排索引
    布尔检索功能
    根据索引次数对返回结果排序
实现方案：
    构造linked list类
    构造图的数据结构，用于存储文档引用关系，统计入度信息；
    用散列存储词典；
    构造term类，用于存储词及其对应文档，对类实现与或非操作；
    构造stack类，用于对输入的查询表达式进行计算；
    构造文档类，用于存储文档id及排序key；
    排序算法，用于对文档排序。